# Database
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=your_password_here
DB_DATABASE=admatch

# JWT (handled by another team member)
JWT_SECRET=your_jwt_secret
JWT_ACCESS_EXPIRATION=1h
JWT_REFRESH_EXPIRATION=30d
